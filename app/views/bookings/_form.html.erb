<%= simple_form_for(@booking) do |f| %>
   <%= f.input :description %>=
   <%= f.button :submit %>
   <%= f.input :meat_id, input_html: { value: @meat.id, type: 'hidden' }%>

  <%= link_to "Back to all Meats", meats_path, class: "btn btn- primary" %>
   <%= f.button :submit, class: "btn btn-danger" %>
<% end %>
